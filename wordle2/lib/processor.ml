let format s = s |> String.uppercase_ascii |> String.trim

let is_valid s = raise Exception "Unimplemented"

let is_valid_naive s = raise Exception "Unimplemented"

let match s1 s2 = raise Exception "Unimplemented"

val t = (char * (int list)) list

let char_index s = raise Exception "Unimplemented"